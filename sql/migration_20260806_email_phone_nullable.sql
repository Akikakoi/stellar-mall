-- ============================================================
-- 邮箱注册改造：phone 列放宽并允许为空
-- （邮箱注册用户没有手机号，phone 不再 NOT NULL）
-- ============================================================
ALTER TABLE stellar_mall_user
    MODIFY COLUMN `phone` VARCHAR(20) NULL COMMENT '手机号（邮箱注册用户为空）';
