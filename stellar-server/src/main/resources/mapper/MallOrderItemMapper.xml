<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================
     订单明细 Mapper（stellar_mall_order_item）
     记录订单内每个商品的快照（名称/规格/单价/数量/小计/附加费/保障服务）。
     支持单条与批量插入、按订单查询（单个/多个订单）。
     ============================================================ -->
<mapper namespace="com.stellar.mapper.MallOrderItemMapper">

    <!-- 数据库字段 → 实体属性映射 -->
    <resultMap id="MallOrderItemMap" type="com.stellar.entity.MallOrderItem">
        <id     property="id"        column="id"/>
        <result property="orderId"   column="order_id"/>
        <result property="spuId"     column="spu_id"/>
        <result property="skuId"     column="sku_id"/>
        <result property="spuName"   column="spu_name"/>
        <result property="skuSpecs"  column="sku_specs"/>
        <result property="price"     column="price"/>
        <result property="qty"       column="qty"/>
        <result property="subtotal"  column="subtotal"/>
        <result property="extraAmount" column="extra_amount"/>
        <result property="serviceInfo" column="service_info"/>
    </resultMap>

    <!-- 新增单条订单明细 -->
    <insert id="insert" parameterType="com.stellar.entity.MallOrderItem"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO stellar_mall_order_item (
            order_id, spu_id, sku_id, spu_name, sku_specs,
            price, qty, subtotal, extra_amount, service_info
        ) VALUES (
            #{orderId}, #{spuId}, #{skuId}, #{spuName}, #{skuSpecs},
            #{price}, #{qty}, #{subtotal}, #{extraAmount}, #{serviceInfo}
        )
    </insert>

    <!-- 批量插入：单条多 VALUES，一次往返写入全部明细 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO stellar_mall_order_item (
            order_id, spu_id, sku_id, spu_name, sku_specs,
            price, qty, subtotal, extra_amount, service_info
        ) VALUES
        <foreach collection="list" item="it" separator=",">
            (#{it.orderId}, #{it.spuId}, #{it.skuId}, #{it.spuName}, #{it.skuSpecs},
             #{it.price}, #{it.qty}, #{it.subtotal}, #{it.extraAmount}, #{it.serviceInfo})
        </foreach>
    </insert>

    <!-- 按订单删除明细（订单重建时使用） -->
    <delete id="deleteByOrderId">
        DELETE FROM stellar_mall_order_item WHERE order_id = #{orderId}
    </delete>

    <!-- 查询单个订单的全部明细 -->
    <select id="listByOrderId" resultMap="MallOrderItemMap">
        SELECT * FROM stellar_mall_order_item
        WHERE order_id = #{orderId}
        ORDER BY id ASC
    </select>

    <!-- 批量查询多个订单的明细（订单列表页一次带出，避免 N+1） -->
    <select id="listByOrderIds" resultMap="MallOrderItemMap">
        SELECT * FROM stellar_mall_order_item
        WHERE order_id IN
        <foreach collection="orderIds" item="orderId" open="(" separator="," close=")">
            #{orderId}
        </foreach>
        ORDER BY order_id ASC, id ASC
    </select>

    <!-- 统计用户是否购买过指定 SPU（关联订单主表，排除待付款/已取消的订单） -->
    <select id="countBoughtByUser" resultType="int">
        SELECT COUNT(*)
        FROM stellar_mall_order_item oi
        JOIN stellar_mall_order o ON o.id = oi.order_id
        WHERE oi.spu_id = #{spuId}
          AND o.user_id = #{userId}
          AND o.status NOT IN ('PENDING', 'CANCELLED')
    </select>

</mapper>