<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stellar.mapper.SmsCodeMapper">

    <resultMap id="SmsCodeMap" type="com.stellar.entity.SmsCode">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="code" column="code"/>
        <result property="type" column="type"/>
        <result property="used" column="used"/>
        <result property="expireTime" column="expire_time"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO stellar_sms_code (phone, code, type, used, expire_time, create_time)
        VALUES (#{phone}, #{code}, #{type}, #{used}, #{expireTime}, #{createTime})
    </insert>

    <select id="findLatest" resultMap="SmsCodeMap">
        SELECT * FROM stellar_sms_code
        WHERE phone = #{phone} AND type = #{type} AND used = 0 AND expire_time > NOW()
        ORDER BY create_time DESC LIMIT 1
    </select>

    <update id="markUsed">
        UPDATE stellar_sms_code SET used = 1 WHERE id = #{id}
    </update>

    <delete id="deleteExpired">
        DELETE FROM stellar_sms_code WHERE expire_time &lt; #{before}
    </delete>
</mapper>
