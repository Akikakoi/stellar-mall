<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================
     订单主表 Mapper（stellar_mall_order）
     覆盖订单全生命周期：创建、状态流转（CAS 防并发）、支付方式、
     发货、退款标记，以及 C 端列表、M 端分页、导出与财务报表。
     状态: PENDING 待付款 / PAID 已付款 / SHIPPED 已发货 / REFUNDED 已退款 / CANCELLED 已取消。
     ============================================================ -->
<mapper namespace="com.stellar.mapper.MallOrderMapper">

    <!-- 数据库字段 → 实体属性映射（user_phone 为导出用联表字段） -->
    <resultMap id="MallOrderMap" type="com.stellar.entity.MallOrder">
        <id     property="id"          column="id"/>
        <result property="orderNo"     column="order_no"/>
        <result property="userId"      column="user_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="payAmount"   column="pay_amount"/>
        <result property="status"      column="status"/>
        <result property="isRefunded"  column="is_refunded"/>
        <result property="address"     column="address"/>
        <result property="consignee"   column="consignee"/>
        <result property="phone"       column="phone"/>
        <result property="payMethod"   column="pay_method"/>
        <result property="remark"      column="remark"/>
        <result property="trackingNo"       column="tracking_no"/>
        <result property="deliveryCompany"  column="delivery_company"/>
        <result property="deliveryTime"     column="delivery_time"/>
        <result property="pointsDeducted" column="points_deducted"/>
        <result property="pointsAmount"   column="points_amount"/>
        <result property="createTime"  column="create_time"/>
        <result property="createUser"  column="create_user"/>
        <result property="updateTime"  column="update_time"/>
        <result property="updateUser"  column="update_user"/>
        <result property="userPhone"  column="user_phone"/>
    </resultMap>

    <!-- 创建订单（含积分抵扣字段） -->
    <insert id="insert" parameterType="com.stellar.entity.MallOrder"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO stellar_mall_order (
            order_no, user_id, total_amount, pay_amount, status,
            address, consignee, phone, pay_method, remark,
            points_deducted, points_amount,
            create_time, create_user, update_time, update_user
        ) VALUES (
            #{orderNo}, #{userId}, #{totalAmount}, #{payAmount}, #{status},
            #{address}, #{consignee}, #{phone}, #{payMethod}, #{remark},
            #{pointsDeducted}, #{pointsAmount},
            #{createTime}, #{createUser}, #{updateTime}, #{updateUser}
        )
    </insert>

    <!-- 更新订单（动态字段，常规修改用） -->
    <update id="update" parameterType="com.stellar.entity.MallOrder">
        UPDATE stellar_mall_order
        <set>
            <if test="orderNo != null">order_no = #{orderNo},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="payAmount != null">pay_amount = #{payAmount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="address != null">address = #{address},</if>
            <if test="consignee != null">consignee = #{consignee},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="payMethod != null">pay_method = #{payMethod},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="pointsDeducted != null">points_deducted = #{pointsDeducted},</if>
            <if test="pointsAmount != null">points_amount = #{pointsAmount},</if>
            update_time = #{updateTime},
            update_user = #{updateUser}
        </set>
        WHERE id = #{id}
    </update>

    <!-- 简化版 pay/cancel 专用：只改 status 不改 update_user 之类业务字段 -->
    <update id="updateStatus">
        UPDATE stellar_mall_order SET status = #{status} WHERE id = #{id}
    </update>

    <!-- CAS 条件状态更新：仅当当前状态 = fromStatus 时才更新为 toStatus，防止支付/取消并发竞态 -->
    <update id="casUpdateStatus">
        UPDATE stellar_mall_order SET status = #{toStatus} WHERE id = #{id} AND status = #{fromStatus}
    </update>

    <!-- 更新支付方式 -->
    <update id="updatePayMethod">
        UPDATE stellar_mall_order SET pay_method = #{payMethod} WHERE id = #{id}
    </update>

    <!-- 发货：设置 SHIPPED 状态 + 物流信息（仅限已付款订单） -->
    <update id="ship">
        UPDATE stellar_mall_order
        SET status = 'SHIPPED',
            tracking_no = #{trackingNo},
            delivery_company = #{deliveryCompany},
            delivery_time = NOW()
        WHERE id = #{id} AND status = 'PAID'
    </update>

    <!-- 按 id 查询订单 -->
    <select id="getById" resultMap="MallOrderMap">
        SELECT * FROM stellar_mall_order WHERE id = #{id} LIMIT 1
    </select>

    <!-- 批量按 id 列表查询订单（调用方需保证 ids 非空，否则 IN () 语法错误） -->
    <select id="listByIds" resultMap="MallOrderMap">
        SELECT * FROM stellar_mall_order
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 按订单号查询 -->
    <select id="getByOrderNo" resultMap="MallOrderMap">
        SELECT * FROM stellar_mall_order WHERE order_no = #{orderNo} LIMIT 1
    </select>

    <!-- C 端：用户全部订单（倒序） -->
    <select id="listByUserId" resultMap="MallOrderMap">
        SELECT * FROM stellar_mall_order
        WHERE user_id = #{userId}
        ORDER BY id DESC
    </select>

    <!-- C 端：用户订单按状态集合过滤（如"待付款/待发货"Tab） -->
    <select id="listByUserIdAndStatus" resultMap="MallOrderMap">
        SELECT * FROM stellar_mall_order
        WHERE user_id = #{userId}
          AND status IN
        <foreach collection="statusList" item="st" open="(" separator="," close=")">
            #{st}
        </foreach>
        ORDER BY id DESC
    </select>

    <!-- M 端：全量分页查询，支持状态/订单号/时间范围过滤（结束日期含当天） -->
    <select id="listAll" resultMap="MallOrderMap">
        SELECT * FROM stellar_mall_order
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="orderNo != null and orderNo != ''">
                AND order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
            <if test="startTime != null and startTime != ''">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt; DATE_ADD(#{endTime}, INTERVAL 1 DAY)
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- M 端：订单总数统计（条件与 listAll 一致） -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM stellar_mall_order
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="orderNo != null and orderNo != ''">
                AND order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
            <if test="startTime != null and startTime != ''">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt; DATE_ADD(#{endTime}, INTERVAL 1 DAY)
            </if>
        </where>
    </select>

    <delete id="deleteById">
        DELETE FROM stellar_mall_order WHERE id = #{id}
    </delete>

    <!-- 标记整单退款完成（幂等：仅未退款且非已取消/已退款的订单可被标记）。
         is_refunded = 0 是数据层幂等防线：重复调用第二次 rows=0，调用方据此跳过库存回滚，
         杜绝重复退款导致库存被回滚两次（库存虚增 → 后续超卖）。
         status != 'CANCELLED' 保持“已取消订单不可退款”的业务约束。 -->
    <update id="markRefunded">
        UPDATE stellar_mall_order
        SET status = 'REFUNDED', is_refunded = 1
        WHERE id = #{id}
          AND is_refunded = 0
          AND status != 'CANCELLED'
          AND status != 'REFUNDED'
    </update>

    <!-- 导出：查询全部订单（关联用户手机号） -->
    <select id="listAllForExport" resultMap="MallOrderMap">
        SELECT o.*, u.phone AS user_phone
        FROM stellar_mall_order o
        LEFT JOIN stellar_mall_user u ON u.id = o.user_id
        <where>
            <if test="status != null and status != ''">AND o.status = #{status}</if>
            <if test="startTime != null and startTime != ''">AND o.create_time &gt;= #{startTime}</if>
            <if test="endTime != null and endTime != ''">AND o.create_time &lt;= #{endTime}</if>
        </where>
        ORDER BY o.create_time DESC
    </select>

    <!-- 财务报表：按月汇总销售额（单量/总金额/实付/退款单量/退款金额，剔除已取消订单） -->
    <select id="financeMonthlySummary" resultType="java.util.HashMap">
        SELECT
            DATE_FORMAT(o.create_time, '%Y-%m') AS month,
            COUNT(o.id) AS order_count,
            COALESCE(SUM(o.total_amount), 0) AS total_amount,
            COALESCE(SUM(o.pay_amount), 0) AS pay_amount,
            COUNT(CASE WHEN o.is_refunded = 1 THEN 1 END) AS refund_count,
            COALESCE(SUM(CASE WHEN o.is_refunded = 1 THEN o.pay_amount ELSE 0 END), 0) AS refund_amount
        FROM stellar_mall_order o
        <where>
            <if test="year != null and year != ''">
                o.create_time BETWEEN CONCAT(#{year}, '-01-01') AND CONCAT(#{year}, '-12-31 23:59:59')
            </if>
            AND o.status != 'CANCELLED'
        </where>
        GROUP BY DATE_FORMAT(o.create_time, '%Y-%m')
        ORDER BY month DESC
    </select>

    <!-- 查询过期的待付款订单（创建时间早于 cutoffTime 且状态为 PENDING） -->
    <select id="listExpiredPending" resultMap="MallOrderMap">
        SELECT * FROM stellar_mall_order
        WHERE status = 'PENDING'
          AND create_time &lt; #{cutoffTime}
        ORDER BY id ASC
    </select>

</mapper>
