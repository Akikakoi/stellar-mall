-- ============================================================
-- V25: 移除 stellar_mall_user.phone 列
-- ------------------------------------------------------------------
-- 背景：
--   C 端登录已完全改为邮箱（email 列由 V8 引入、V14 补齐既有账号），
--   phone 不再是登录账号，也不在后端/前端任何地方使用。
--   本迁移从表结构层面移除该列。
--
-- 说明：
--   1. phone 曾是唯一索引 uk_mall_user_phone 的唯一列；DROP COLUMN 后
--      MySQL 会自动移除该单列索引，无需单独 DROP INDEX。
--   2. email 为登录账号，唯一约束由新库初始化 DDL 保证；
--      存量库如已有 email，此处不重复加约束以避免重复邮箱导致迁移失败。
-- ============================================================

ALTER TABLE stellar_mall_user DROP COLUMN `phone`;